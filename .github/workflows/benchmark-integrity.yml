name: benchmark-integrity

# Block drift between ground-truth and the analysis code at CI time.
on:
  push:
    paths:
      - 'examples/case-studies/**/truth.json'
      - 'examples/case-studies/self-evaluation/case-01/evidence_root/**'
      - 'examples/sample-evidence/**'
      - 'dart_mcp/**'
      - 'scripts/benchmark/**'
  pull_request:
    paths:
      - 'examples/case-studies/**/truth.json'
      - 'examples/case-studies/self-evaluation/case-01/evidence_root/**'
      - 'examples/sample-evidence/**'
      - 'dart_mcp/**'
      - 'scripts/benchmark/**'

jobs:
  integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Validate ground-truth integrity
        # evidence_path existence + expected_function registration + host/evidence
        # consistency. Non-zero exit on FAIL blocks the merge.
        run: python3 scripts/benchmark/validate_ground_truth.py

      - name: Regression - score case-01 (deterministic)
        # Guard the scorer against breakage. deterministic emits a fixed case-01
        # output, so strict recall must reproduce a known value.
        env:
          PYTHONPATH: dart_audit/src:dart_mcp/src:dart_agent/src:dart_corr/src
          DART_EVIDENCE_ROOT: examples/case-studies/self-evaluation/case-01/evidence_root
        run: |
          python3 -m dart_agent --case self-evaluation/case-01 --out /tmp/c01 --mode deterministic
          python3 scripts/benchmark/score_cases.py --case self-evaluation/case-01 --run-dir /tmp/c01 --json
