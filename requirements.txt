# Agentic-DART third-party runtime dependencies.
#
# These mirror the lower bounds declared in the per-package pyproject.toml
# files (dart_audit / dart_mcp / dart_agent / dart_corr). The local packages
# themselves are installed editable by scripts/install.sh AFTER these wheels:
#
#     pip install -r requirements.txt
#     pip install -e ./dart_audit -e './dart_mcp[stdio]' \
#                 -e ./dart_corr  -e './dart_agent[live]'
#
# dart_audit has no third-party runtime dependencies (stdlib only).

# dart_agent (live mode) — Anthropic SDK + MCP client + HTTP
anthropic>=0.40.0
mcp>=1.0.0
requests>=2.25.0

# dart_mcp — correlation store, registry hive parsing, MCP server
# duckdb bound is the intersection of dart_mcp (>=1.5.3) and dart_corr (>=0.10.0)
duckdb>=1.5.3,<2.0
python-registry>=1.3.1

# dart_corr — playbook parsing
PyYAML>=6.0
