# Agentic-DFIR third-party runtime dependencies.
#
# These mirror the lower bounds declared in the per-package pyproject.toml
# files (dfir_audit / dfir_mcp / dfir_agent / dfir_corr). The local packages
# themselves are installed editable by scripts/install.sh AFTER these wheels:
#
#     pip install -r requirements.txt
#     pip install -e ./dfir_audit -e './dfir_mcp[stdio]' \
#                 -e ./dfir_corr  -e './dfir_agent[live]'
#
# dfir_audit has no third-party runtime dependencies (stdlib only).

# dfir_agent (live mode) — Anthropic SDK + MCP client + HTTP
anthropic>=0.40.0
mcp>=1.0.0,<2
requests>=2.25.0

# dfir_mcp — correlation store, registry hive parsing, MCP server
# duckdb bound is the intersection of dfir_mcp (>=1.5.3) and dfir_corr (>=0.10.0)
duckdb>=1.5.3,<2.0
python-registry>=1.3.1

# dfir_corr — playbook parsing
PyYAML>=6.0
